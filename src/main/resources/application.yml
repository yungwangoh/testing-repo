spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: qwer1234
    url: jdbc:postgresql://localhost:5431/test
  ai:
    openai:
      api-key: ${API_KEY}
